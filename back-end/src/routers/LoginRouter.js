import { Router } from 'express';

const loginRouter = Router();
// const loginController = new LoginController();

// Req 4

loginRouter.post(
   '/login',
   /* (req, res) => loginController.verifyUser(req, res),
   Vinicius criou o UserService de forma que verifica se a formatação dos campos estão ok, e então procura se esses dados estão no banco de dados
   */
);