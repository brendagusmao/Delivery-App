import { createContext } from 'react';
// O context foi criado aqui porque não funciona no sistema se criado junto ao arquivo Provider.js
const AppContext = createContext();
export default AppContext;
